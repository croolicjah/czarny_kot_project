from .models import Post
# from django import forms
from tinymce.widgets import TinyMCE
from django.db import models

# class PostForm(forms.ModelForm):
#     class Meta:
#         model = Post  # Specify your model
#         fields = '__all__'  # Or list the fields you want to include
#     # Add a widget to the description field
#     lead_carousel_dot = forms.CharField(widget=forms.Textarea(attrs={'class': 'nic-edit','cols': 80}))
#     lead_feat = forms.CharField(widget=forms.Textarea(attrs={'class': 'nic-edit','cols': 80}))
#     content = forms.CharField(widget=forms.Textarea(attrs={'class': 'nic-edit','cols': 80}))


